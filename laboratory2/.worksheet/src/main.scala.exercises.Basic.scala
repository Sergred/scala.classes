package main.scala.exercises

/**
 * Second round of warm up exercises on:
 * - Recursion.
 * - Recursion with lists.
 * - Pattern matching.
 * - Anonymous functions
 * - Higher order functions.
 *
 * One rule: Only immutable variables.
 */
object Basic {

  type ??? = Nothing;import org.scalaide.worksheet.runtime.library.WorksheetSupport._; def main(args: Array[String])=$execute{;$skip(620); val res$0 = 

  /**
   * -- Exercise --
   * Create a function "append" which receives two lists of integers and returns
   * a new list containing the elements of both.
   * Test:
   * - append(List(1, 2, 3), List(4, 5, 6)) = List(1, 2, 3, 4, 5, 6)
   * - append(List(), List(1, 2)) = List(1, 2)
   * - append(List(3, 4), List()) = List(3, 4)
   */
  ???;System.out.println("""res0: Nothing = """ + $show(res$0))}

  /**
   * -- Exercise --
   * Create a function "reverse" which receives a list of characters and returns
   * a new list with the elements in a reversed order.
   * Test:
   * - reverse(List('f', 'b', 'd', 'e')) = List('e', 'd', 'b', 'f')
   * - reverse(List()) = List()
   * - reverse("edvoks".toList) = List('s', 'k', 'o', 'v', 'd', 'e')
   */

}
